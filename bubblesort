def sort(nums):
    for i in range(len(nums)-1,0,-1):
        for j in range(i):
            if nums[j]>nums[j+1]:
                temp=nums[j]
                nums[j]=nums[j+1]
                nums[j+1]=temp
       
nums=[11,56,5,6,2,8,1,6,1,9,10]
sort(nums)
print("sorted list-")
print(nums)
